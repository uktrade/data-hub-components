import { ACTIVITY_TYPE_FILTERS } from '../../constants'

export const activityTypeFilterPropsFixture = {
  values: ACTIVITY_TYPE_FILTERS,
  defaultValue: ACTIVITY_TYPE_FILTERS[3].value,
}

export default {
  activityTypeFilterPropsFixture,
}
